import { Components } from '@mui/material/styles';
import { IThemeOptions } from '../theme-options';

const muiButton = (_theme: IThemeOptions): Components['MuiButton'] => ({});

export default muiButton;
